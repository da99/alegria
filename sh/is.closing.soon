#!/usr/bin/env zsh
#
#
#
set -u -e

if test $(hostname) = "v01" ; then
  set -x
fi

cd $HOME/apps/alegria

now_24="$(date +"%H")"
closing_24=$(sh/what.hour24.closing)
minute="$(date +"%M")"

if test "$now_24" -eq $(( $closing_24 - 1)) && test "$minute" -gt 27 ; then
  exit 0
fi

exit 1

