#!/usr/bin/env zsh
#
#
set -u -e -o pipefail

cd $HOME/apps/alegria

while sh/is.running ; do
  if sh/is.closing.soon ; then
    echo "%{c}%{T3}are"
  else
	  if sh/is.open ; then
		  echo "%{c}%{T4}seu"
	  else
		  echo "%{c}%{T5}are"
	  fi
  fi
  sh/sleep.minute
done

