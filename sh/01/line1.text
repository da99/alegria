#!/usr/bin/env zsh
#
#
set -u -e -o pipefail

cd $HOME/apps/alegria

while sh/is.running ; do
  if sh/is.closing.soon ; then
    echo "%{c}%{T5}We"
  else
	  if sh/is.open ; then
		  echo "%{c}%{T5}Faça"
	  else
		  echo "%{c}%{T5}We"
	  fi
  fi
  sh/sleep.minute
done

