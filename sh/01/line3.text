#!/usr/bin/env zsh
#
#
set -u -e -o pipefail

cd $HOME/apps/alegria

while sh/is.running ; do
  if sh/is.closing.soon ; then
    echo "%{c}%{T3}closing"
  else
	  if sh/is.open ; then
		  echo "%{c}%{T4}pedido"
	  else
		  echo "%{c}%{T5}closed."
	  fi
  fi
  sh/sleep.minute
done

